# 移动与渲染性能优化

2026-09-06，针对移动迟滞与低帧率完成优化。

## 本机实测

1440 × 1000，Microsoft Edge，Intel Arc / Direct3D 11，快速人机战；使用相同脚本，预热后连续移动并射击约 6 秒。

| 指标                    | 优化前   | 优化后   |
| ----------------------- | -------- | -------- |
| 平均帧率                | 34.1 FPS | 59.3 FPS |
| 每帧平均 WebGL 绘制调用 | 807      | 157      |
| 帧间隔 P95              | 66.6 ms  | 16.8 ms  |

性能优化阶段的最终一轮在原生 1× 分辨率下，绘制调用减少约 81%，平均帧率提高约 74%。原始记录：[优化前](docs/performance/before.json)、[最终复测](docs/performance/final.json)。测试时尚未加入进阶武装；新特效另进行绘制预算和功能回归。短时测试包含随机战斗与系统负载，不保证所有设备达到相同帧率。

## 改动

- **合并刚性几何**：履带、车轮、装甲等相同材质零件合并绘制，保留模型轮廓、材质以及炮塔的独立旋转。
- **阴影与分辨率**：软阴影采用 1024² 贴图、最高每秒 20 次更新。持续低帧率时自动降低渲染分辨率，恢复时使用较长观察窗口，避免反复变化。
- **即时移动反馈**：本机坦克根据当前按键进行最多 100ms 的视觉预测，使用与服务端相同的碰撞代码。每次权威状态更新替换预测基准；过期快照与阵亡状态禁用预测。
- **服务器计时**：按实际经过时间累计固定模拟步长，定时器偶尔延迟时补算，避免整个世界随服务器忙碌而减速；长时间挂起的补算有上限。
- **减少分配与界面重绘**：复用炮弹对象、临时向量和音效缓冲，缓存姓名牌 DOM，降低 HUD 更新频率，移除动态战场上多层背景模糊。
- **开发环境**：Vite 仅以 `index.html` 扫描入口，排除测试报告、截图、构建和文档产物；重新启动开发服务，清理此前约 4 GB 的进程内存占用。

坦克基础移动速度、伤害与对战规则保持原设计。命中、碰撞、补给、比分仍由服务端判定。

## 验证与边界

新增 8 项性能测试覆盖计时抖动、延迟回调、预测碰撞、阵亡 / 断网状态、几何合并的坐标正确性、炮塔独立旋转和自动分辨率，并增加 1 项突发输入包网络回归。连同原有测试共 40 项全部通过，3 组浏览器端到端回归与生产构建通过。

测试脚本：

```sh
node scripts/profile-game.mjs --name=local
npm test
npm run test:e2e
npm run build
```

`--software` 可单独检查强制 SwiftShader 路径。纯软件 WebGL 在本机仍存在明显帧率限制。推荐使用开启硬件加速的 Chrome / Edge 运行实时 3D 战场。硬件加速的收益不能外推到纯软件渲染，也未执行跨地区网络性能压测。

## 据点与充足补给扩展复测

同日以 1440 × 1000、Edge / Intel Arc、原生 1× 分辨率，在默认据点模式（1 名玩家 + 3 名电脑、24 处补给）预热后持续移动射击约 6 秒：平均 **57.0 FPS**，帧间隔 P95 **17.0 ms**，每帧平均 **265 次绘制调用**，无 JavaScript / 控制台错误。记录见 [tactics-movement.json](docs/performance/tactics-movement.json)。这是当前版本的一次短时实测，不是旧版 59.3 FPS 数据的沿用。

另外，八车进阶武装、24 处补给、7 个战术区域同时存在的渲染夹具采样为 **392 次绘制调用**，通过 500 次预算检查，见 [tactics.json](docs/performance/tactics.json)。补给共享几何与图标纹理，名称仅显示最近两处；烟幕采用实例化烟团，据点进度复用几何绘制范围。烟幕最多 8 团，伤害场最多 8 处，瞬时战术特效最多 64 个。

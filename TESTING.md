# 验收记录

验证日期：2026-09-06。环境：Windows、Node.js 23.5.0、npm 10.9.2、Microsoft Edge / Playwright。最初进行软件 WebGL 功能检查，性能修复后的完整回归使用 Intel Arc 硬件加速。

## 结果

据点与补给扩展后：**94 项规则 / 网络 / 性能 / 道具 / 操作测试通过，9 组浏览器端到端场景通过**。新玩法设计见 [docs/TACTICS.md](docs/TACTICS.md)。浏览器回归使用独立后端 3003 与 Vite 5175，避免与用户正在使用的固定房间 12345 争用。

| 检查     | 结果         | 验证内容                                                                                            |
| -------- | ------------ | --------------------------------------------------------------------------------------------------- |
| 引擎     | 25 / 25 通过 | 移动碰撞、基础武器、增益、地雷、复活、AI、结算                                                      |
| 联机集成 | 12 / 12 通过 | 固定房间、权限、幂等创建、回收、输入、重赛、场地同步、平衡分队与据点同步                            |
| 性能回归 | 8 / 8 通过   | 固定计时、预测碰撞、几何合并、自动分辨率                                                            |
| 进阶武器 | 11 / 11 通过 | 连锁、反弹、集束、引力、轰炸、冰霜与数量上限                                                        |
| 操作     | 9 / 9 通过   | 多输入源独立、短按保留、失焦停止、辅助瞄准                                                          |
| 战术规则 | 29 / 29 通过 | 夺点、争夺、反占、积分、友军免伤、烟幕与侦察、三图电脑夺点、24 处补给与刷新                         |
| 浏览器   | 9 / 9 通过   | 桌面/触屏控制、两客户端联机、16 种道具、F/G 技能、模式切换、烟幕隐藏/侦察显示、补給倒计时与渲染预算 |
| 构建     | 通过         | TypeScript 与 Vite 前端 / Node 服务生产构建                                                         |
| 格式     | 通过         | Prettier 检查                                                                                       |

浏览器自动化检查未发现 JavaScript 错误，旧有桌面流程继续验证运行时无外部资源请求。24 处补给与八车特效的绘制样本见 [docs/performance/tactics.json](docs/performance/tactics.json)，它是本机渲染预算检查，不等同于多客户端网络吞吐量测试。

## 实测并修复的问题

- **短促按键丢失**：地雷按下 / 松开的事件可能在下一次同步前结束。客户端保存瞬时操作，服务端在模拟消费前保留操作，新增回归测试。
- **穿甲弹重复伤害**：高速弹药在碰撞子步内可能多次计算同一箱体伤害。现在对每个弹体 / 目标组合只计算一次。
- **双摇杆互相干扰**：松开移动摇杆不再错误地清除触屏射击状态。
- **竖屏战车偏离视野中心**：竖屏使用更贴近玩家的跟随范围。
- **外部字体依赖**：字体已经按界面文本生成本地子集，浏览器实测没有跨域资源请求。

## 视觉验收

已检查 1440px 桌面车库、房间准备室、真实 3D 对战，以及 390 × 844 触屏页面。移动页面没有横向溢出，摇杆、战斗菜单和战术按钮可操作；烟幕与侦察按钮不覆盖摇杆。另检查了 844 × 390 横屏战场。

- [据点与战术 HUD](docs/screenshots/tactics-desktop.webp)
- [手机战术操作](docs/screenshots/tactics-mobile.webp)
- [补给视觉与倒计时](docs/screenshots/supplies.webp)
- [车库截图](docs/screenshots/garage.webp)
- [房间截图](docs/screenshots/room.webp)
- [对战截图](docs/screenshots/battle.webp)
- [手机车库](docs/screenshots/mobile-garage.webp)
- [手机对战](docs/screenshots/mobile-battle.webp)

## 复现

```sh
npm ci
npm test
npm run build
npm run test:e2e
npm run format:check
```

生产冒烟验证（先在另一个终端运行 `npm start`）：

```sh
node scripts/smoke-production.mjs http://127.0.0.1:3001
```

将 `PLAYWRIGHT_BASE_URL` 设置为生产服务地址，可运行 `npx playwright test tests/e2e/game.spec.ts tests/e2e/controls.spec.ts` 验证生产服务。进阶武器与战术文件中的渲染夹具会动态导入 TypeScript，完整 9 场景应使用 Vite。详细命令与环境说明见 [README.md](README.md)。

## 已知范围

- 未执行公网部署、跨地区弱网压测或实体手机 / iOS Safari 测试。另已补充本机 Intel Arc 的短时性能对比，未进行多机型正式性能基准。
- Dockerfile 与 GitHub Actions 配置已提供，本次未运行 Docker 构建或远程 CI。
- 烟幕是普通客户端的玩法可见性规则，姓名牌、模型、小地图与辅助瞄准遵循同一判定；服务端仍广播完整模拟快照，未提供防读取位置的商业反作弊。
- 当前是单进程内存房间；没有账号、持久战绩、跨服匹配或断线续局。断线会退出当前房间，重新连接后可重新创建 / 加入准备中的房间。
- AI 采用网格寻路、夺点/补给目标选择、烟幕可见性与不同瞄准精度，不使用外部大模型 API。

# IRONCLAD · 钢铁边境

## 产品与体验设计（实现前设计）

浏览器中的即时 3D 坦克竞技游戏。打开即玩，不要求注册。首屏就是充满机械细节的作战车库：米白编辑式排版、橙色行动按钮、暗色坦克主视觉和沙漠地图卡。中文为主，少量英文作为军事装备标识。桌面键鼠是主要操作方式，同时提供触屏双摇杆。

### 一局的核心循环

选择战车 → 快速人机战或创建 / 加入房间 → 争夺补给、利用掩体、移动瞄准 → 击毁敌人、复活再战 → 达到击杀目标或时间结束 → 排行榜与再来一局。

默认每局 3 分钟，率先 12 次击毁获胜；最多 8 名战车玩家（真人 + AI）。房间所有真人都能看到同一份战场状态。房主可以开始比赛、增加 / 移除电脑、调整地图、AI 难度与目标分数。支持房主离开后的转移、断线返回提示和空房间清理。

### 操作与反馈

- WASD / 方向键移动，鼠标独立瞄准，左键 / 空格连续开火。
- Shift 冲刺：短时加速、冷却约 4 秒；E 放置地雷：最多携带 3 枚。
- 移动端：左摇杆移动，右摇杆瞄准并射击，独立冲刺与地雷按钮。
- 受击闪光、炮口火焰、拖尾、弹壳 / 火星粒子、爆炸环、屏幕震动、合成空间音效、击毁播报。
- 玩家标记、血量、武器剩余时间、冲刺冷却、比赛时间、排行榜和小地图始终可读。
- Esc 打开设置 / 操作帮助，Tab 查看比分；静音和减少动态效果可持久保存。

### 战车

| 车型            | 定位     | 最大生命 | 速度 | 基础射击间隔 |
| --------------- | -------- | -------- | ---- | ------------ |
| 游隼 / VANGUARD | 均衡突击 | 100      | 7.0  | 0.43s        |
| 幽灵 / GHOST    | 高速侦察 | 75       | 9.0  | 0.38s        |
| 壁垒 / BASTION  | 重装压制 | 150      | 5.5  | 0.56s        |

### 补给系统

10 种可拾取补给，拾取即生效：三连散射、速射机炮、穿甲轨道炮、追踪导弹、烈焰炮、护盾、极速、维修、地雷弹药、双倍伤害。武器与增益有清晰的时限反馈。战场固定补给点定时随机刷新，鼓励移动而非蹲守。地雷有引信延迟、范围爆炸与归属。

### 地图与 AI

三张具有不同掩体布局与地面色调的地图：赤沙哨站、极夜基地、翠谷遗迹。地图中有不可破坏的岩体 / 钢墙、可破坏箱体、爆炸油桶。所有出生点与补给点不得置于障碍物中。

AI 使用网格寻路绕开障碍，追逐可见敌人 / 补给，短距避障，预测瞄准，视线检查和不同精度 / 射击频率。简单 / 标准 / 困难三档，复活后有短时保护。

## 美术方案

- 车库：暖白 #f3f2ed、墨黑 #202420、信号橙 #fa5b32，技术网格与细线装饰，非霓虹风。
- 主视觉：具有磨损装甲与履带细节的军绿坦克，沙漠尘土与远处工业遗迹；Google 生成图仅作车库背景，不烘焙界面文字。
- 实战：Three.js 实时几何场景，立体坦克 / 炮塔 / 履带、软阴影、地面细节、氛围雾、发光道具和有辨识度的粒子效果。素材缺失也能完整运行。
- 交互：明显的按钮状态、简短过渡、页面适配；避免游戏中大面积装饰遮挡视线。

## 技术方案

- TypeScript + React + Vite：界面、菜单、状态与输入。
- Three.js：实时 3D 渲染；渲染逻辑和游戏模拟分离。
- Node.js + Socket.IO：同源 HTTP / WebSocket 服务，30Hz 权威模拟，15Hz 状态广播；客户端平滑插值，输入约 30Hz。
- 共享纯 TypeScript 引擎：物理、伤害、寻路、补给和胜负判定可独立自动测试。客户端只发送受约束的输入，不提交伤害或分数。
- 临时房间保存在内存；开发模式 Vite 代理，生产模式一个 Node 服务托管前端与联机。
- Google 图片生成脚本只在本机 / 服务端读取 GOOGLE_API_KEY，不进入前端代码或响应。生成结果保存在 public/assets，记录提示词并提供可复现命令。

## 验收与边界

1. TypeScript 类型检查与生产构建通过。
2. 引擎测试覆盖移动碰撞、射击伤害、击毁计分、复活保护、补给、地雷、AI 移动、时间与分数结算。
3. Socket 集成测试用两个独立客户端验证房间、加入、AI、开始、状态一致、权限校验、断线与再战。
4. 浏览器验证车库 → 快速对战 → 移动开火 → 返回，以及双页面联机、窄屏布局、控制台错误和截图检查。
5. 交付运行命令、联机方法、测试报告、素材来源与限制。

这是可部署的单进程游戏版本。公网联机需要部署到一台可访问的 Node 服务器；没有账号、跨服匹配、数据库或商业反作弊系统，不将这些未实现能力展示为可用功能。

## 后续道具扩展

进阶武装的设计与验收见 [docs/POWERUPS.md](docs/POWERUPS.md)。当前版本增加了雷霆电弧、棱镜弹射炮、奇点引力弹、蜂群集束弹、天基轰炸与冰霜新星，道具总数为 16。

## 据点与补给扩展

最新玩法设计见 [docs/TACTICS.md](docs/TACTICS.md)，在本轮实现前写入。默认模式更新为双队据点争夺，原自由混战保留；新增烟幕、共享侦察、分队 AI、24 处快速补充的资源站，以及统一的键鼠与触屏操作。
